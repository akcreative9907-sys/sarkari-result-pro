import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
export async function GET(request: Request) { const { searchParams } = new URL(request.url); const q = searchParams.get("q") || ""; const type = searchParams.get("type") || undefined; const state = searchParams.get("state") || undefined; const qualification = searchParams.get("qualification") || undefined; const posts = await prisma.post.findMany({ where: { status: "PUBLISHED", ...(type ? { type: type as never } : {}), ...(state ? { state } : {}), ...(qualification ? { qualification: { contains: qualification, mode: "insensitive" } } : {}), OR: q ? [{ title: { contains: q, mode: "insensitive" } }, { organization: { contains: q, mode: "insensitive" } }, { summary: { contains: q, mode: "insensitive" } }] : undefined }, include: { category: true, trending: true }, orderBy: [{ publishedAt: "desc" }], take: 30 }); return NextResponse.json({ posts }); }
