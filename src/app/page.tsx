import Link from "next/link";
import { ArrowRight, Flame, Radio, Sparkles, TrendingUp } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { SearchBar } from "@/components/site/search-bar";
import { AdZone } from "@/components/site/ad-zone";
import { PostCard } from "@/components/cards/post-card";
import { AnimatedPanel } from "@/components/site/animated-panel";
import { categories, featuredPosts } from "@/data";
export default function HomePage() { return <><section className="section grid min-h-[calc(100vh-4rem)] items-center gap-10 lg:grid-cols-[1.05fr_.95fr]"><div><Badge className="mb-5 bg-primary/10 text-primary"><Radio className="mr-1 h-3 w-3" /> Live auto-updates enabled</Badge><h1 className="max-w-4xl text-4xl font-bold tracking-tight md:text-6xl">Government jobs intelligence, rebuilt for speed and clarity.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-muted-foreground">Latest jobs, results, admit cards, answer keys, admissions, syllabus, AI summaries, trend scores, bookmarks, and official-source alerts in one premium Sarkari platform.</p><div className="mt-8"><SearchBar /></div><div className="mt-6 flex flex-wrap gap-3"><Button asChild size="lg"><Link href="/jobs">Explore latest jobs <ArrowRight className="h-4 w-4" /></Link></Button><Button asChild size="lg" variant="outline"><Link href="/results">Check results</Link></Button></div></div><div className="glass rounded-lg p-5 shadow-glow"><div className="grid gap-4">{["Trending Today", "Closing Soon", "Latest Results"].map((label, i) => <div key={label} className="rounded-md border bg-background/70 p-4"><div className="flex items-center justify-between"><p className="font-semibold">{label}</p><TrendingUp className="h-4 w-4 text-primary" /></div><p className="mt-2 text-sm text-muted-foreground">{featuredPosts[i]?.title}</p></div>)}</div></div></section><section className="section pt-0"><div className="mb-6 flex items-center justify-between"><div><h2 className="text-2xl font-bold">Latest verified updates</h2><p className="text-muted-foreground">AI-enriched from official sources and queued for editorial approval.</p></div><Flame className="h-6 w-6 text-primary" /></div><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">{featuredPosts.map((post, index) => <AnimatedPanel key={post.slug} delay={index * 0.04}><PostCard post={post} /></AnimatedPanel>)}</div></section><section className="section pt-0 grid gap-4 lg:grid-cols-[1fr_320px]"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{categories.map((cat) => <Link href={`/search?category=${cat.slug}`} key={cat.slug} className="glass rounded-lg p-4 transition hover:-translate-y-1"><Sparkles className="mb-3 h-5 w-5 text-primary" /><p className="font-semibold">{cat.name}</p><p className="mt-2 line-clamp-3 text-sm leading-6 text-muted-foreground">{cat.description}</p></Link>)}</div><AdZone label="Premium" /></section></>; }


